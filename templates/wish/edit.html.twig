{% extends "base.html.twig" %}

{% block titre %}
    Modifier un souhait
{% endblock %}

{% block contenu %}
    <div class="container">
        <div class="col-6">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <br>
                <button type="submit" class="btn btn-dark">Modifier</button>
            {{ form_end(form) }}
        </div>
    </div>
    
{% endblock %}